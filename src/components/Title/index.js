import React from "react";


export const Title = ({greeting}) =>{
    //console.log(props);
    return(
        
         <h1 className="h1">{greeting}</h1>
        
       
    );
}

export default Title;